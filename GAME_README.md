# クッキークリッカー風ゲーム（おみくじ版がベース）

おみくじアプリと同じ「3層構成」の考え方を使って、クリック＋アップグレードのクッキークリッカー風ゲームを作ります。
実装コードはこのファイルには書きません。設計の方針と課題だけをまとめています。

---

## ゲームのルール（今回作る範囲）

- 画面の「クッキーを焼く」ボタンを押すと、クッキー数が増える。
- 貯まったクッキーで「アップグレード」を購入できる。
- アップグレードを買うと、1クリックあたりに増えるクッキー数が上がる。
- 自動生成（放置していても増える仕組み）は今回は**やらない**（ステップ3以降の自由課題では候補にしてもよい）。

---

## 全体の構成（3層）

おみくじ版と同じ分け方を踏襲します。ファイル名は自由に決めてよいですが、例として以下を想定しています。

| ファイル | 層 | 役割 |
| --- | --- | --- |
| `src/game.ts`（仮） | データ層 | クッキー数・アップグレードの状態と、状態を変える関数（クリック処理・購入処理） |
| `src/render.ts` | 描画層 | 状態を画面(DOM)に表示する（クッキー数、アップグレードボタンの表示） |
| `src/main.ts` | イベント層 | ボタンのクリックとデータ層の関数を結びつける |

おみくじ版との対応:

- `omikuji.ts`（くじの型・比率・引く関数） → `game.ts`（クッキーの状態・アップグレードの定義・購入ロジック）
- `render.ts`（結果を表示） → `render.ts`（クッキー数・アップグレード一覧を表示）
- `main.ts`（ボタンとロジックを結びつける） → `main.ts`（クリック・購入ボタンとロジックを結びつける）

データ層は外部から状態を直接書き換えられないようにする（おみくじ版で `tickets` を `export` しなかったのと同じ考え方）。

---

## ステップ1（必修）：データ層を設計する

`game.ts`（ファイル名は自分で決めてよい）に以下を用意する。

1. 状態を表す型または変数
   - 今持っているクッキー数
   - 1クリックあたりに増える量
   - アップグレードの一覧（例: 名前・値段・効果・購入済みかどうか、または購入回数）
2. クッキーを1クリック分増やす関数
3. アップグレードを購入する関数
   - クッキー数が値段以上あるかを確認する
   - 足りなければ何もしない（または購入失敗を知らせる）
   - 足りていれば、クッキー数から値段を引き、効果（1クリックあたりの増加量アップなど）を適用する

考えどころ:
- アップグレードは1回買ったら終わりにするか、何度でも買えるようにして値段を毎回上げるか（後者のほうがクッキークリッカーらしいが、最初は前者の「1回だけ買える」で十分）。
- アップグレードの情報（名前・値段・効果）をどんな型・データ構造で持たせるか（オブジェクトの配列がおすすめ）。

---

## ステップ2（必修）：描画層を実装する

`render.ts` に以下を実装する。

1. 現在のクッキー数を画面に表示する関数
2. アップグレードの一覧を表示する関数
   - 値段や名前を表示する
   - 購入済み（または買えない状態）なら見た目やボタンの状態を変える（例: `disabled` にする）

ヒント: おみくじ版と同様、`getElementById` / `textContent` / `createElement` / `appendChild` の範囲で実装できる。

---

## ステップ3（必修）：イベント層でつなぐ

`main.ts` に以下を実装する。

1. 「クッキーを焼く」ボタンのクリック → データ層の「クリック処理」を呼ぶ → 描画層の「クッキー数表示」を呼ぶ
2. 各アップグレードの購入ボタンのクリック → データ層の「購入処理」を呼ぶ → 描画層の「クッキー数表示」と「アップグレード一覧表示」を両方呼び直す

---

## ステップ4以降：自由拡張（例）

ここからは自由。難易度別の例:

### 易
- アップグレードの見た目・文言・色を変える。
- クッキー数の表示にカンマ区切り（例: 1,000）を入れる。

### 中
- アップグレードを何度でも買えるようにし、購入するたびに値段を上げる（例: 前回の値段 × 1.15）。
- 「所持数」や「合計で稼いだクッキー数」を表示する。

### 難
- 自動生成（一定時間ごとにクッキーが増える）の仕組みを追加する（`setInterval` を使う）。
- `localStorage` を使ってセーブ・ロードできるようにする。
- 実績（例: 「100枚達成」）を表示する仕組みを追加する。

---

## 使ってよい技術・避ける技術

おみくじ版と同じ方針。

- 使うもの: TypeScript（型・関数・ファイル分割）、基本の HTML タグ、DOM 操作（`getElementById` / `textContent` / `createElement` / `appendChild` など）、`addEventListener`。慣れてきたら `setInterval` / `localStorage` も可。
- 避けるもの: React/Vue などのフレームワーク。
